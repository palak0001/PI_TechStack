# 🏗️ Complete Project Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        PI TECHSTACK WEBSITE                      │
│                     (Digital Agency Website)                     │
└─────────────────────────────────────────────────────────────────┘
                                │
                 ┌──────────────┴──────────────┐
                 │                             │
          ┌──────▼──────┐            ┌────────▼────────┐
          │  FRONTEND   │            │    BACKEND      │
          │  (React)    │            │  (Express.js)   │
          └──────┬──────┘            └────────┬────────┘
                 │                             │
        ┌────────┴────────┐         ┌─────────┴─────────┐
        │                 │         │                   │
    Pages (6)       Components (3)  Routes (2)    Database (1)
        │                 │         │                   │
    ├─Home         ├─Navbar       ├─POST /api/contact  │
    ├─About        ├─Footer       └─GET /api/contacts  │
    ├─Services     └─Common                      MongoDB Schema
    ├─Projects         │                              │
    ├─Testimonials     └─(Button, Card,         Contact:
    └─Contact            Modal, Badge)          ├─name
                                               ├─email
                                               ├─phone
                                               ├─message
                                               ├─status
                                               └─timestamps
```

---

## Frontend Architecture (React)

```
┌─── FRONTEND (React + Vite) ───────────────────┐
│                                               │
│  ┌── Entry Point ──────────────────────────┐ │
│  │  index.html → main.jsx → App.jsx       │ │
│  └──────────────┬──────────────────────────┘ │
│                 │                             │
│  ┌──────────────▼──────────────────────────┐ │
│  │  React Router Configuration             │ │
│  │  ├─ / → Home                            │ │
│  │  ├─ /about → About                      │ │
│  │  ├─ /services → Services                │ │
│  │  ├─ /projects → Projects                │ │
│  │  ├─ /testimonials → Testimonials        │ │
│  │  └─ /contact → Contact                  │ │
│  └──────────────┬──────────────────────────┘ │
│                 │                             │
│  ┌──────────────▼──────────────────────────┐ │
│  │  Layout Components                      │ │
│  │  ├─ Navbar (sticky)                     │ │
│  │  ├─ Page Content                        │ │
│  │  └─ Footer (sticky)                     │ │
│  └──────────────┬──────────────────────────┘ │
│                 │                             │
│  ┌──────────────▼──────────────────────────┐ │
│  │  Reusable Components                    │ │
│  │  ├─ Button (primary, secondary, outline)│ │
│  │  ├─ Card (with hover effects)           │ │
│  │  ├─ Modal (service details)             │ │
│  │  └─ Badge (tech stack labels)           │ │
│  └──────────────┬──────────────────────────┘ │
│                 │                             │
│  ┌──────────────▼──────────────────────────┐ │
│  │  Styling & Animations                   │ │
│  │  ├─ Tailwind CSS (utility classes)      │ │
│  │  ├─ index.css (global styles)           │ │
│  │  └─ Framer Motion (animations)          │ │
│  └──────────────────────────────────────────┘ │
│                                               │
│  ┌──────────────────────────────────────────┐ │
│  │  Data Management                         │ │
│  │  └─ data/content.js (demo data)          │ │
│  └──────────────────────────────────────────┘ │
│                                               │
└───────────────────────────────────────────────┘
```

---

## Backend Architecture (Express.js)

```
┌─── BACKEND (Node.js + Express) ──────────────┐
│                                              │
│  ┌── Server Entry ────────────────────────┐ │
│  │  server.js                             │ │
│  │  ├─ Initialize Express                │ │
│  │  ├─ Connect to MongoDB                │ │
│  │  ├─ Setup middleware (CORS, JSON)     │ │
│  │  └─ Listen on port 5000                │ │
│  └──────────────┬────────────────────────┘ │
│                 │                           │
│  ┌──────────────▼────────────────────────┐ │
│  │  Routes & Endpoints                   │ │
│  │  ├─ POST /api/contact                │ │
│  │  │  └─ Submit contact form           │ │
│  │  ├─ GET /api/contacts                │ │
│  │  │  └─ Get all submissions           │ │
│  │  ├─ GET /api/health                  │ │
│  │  │  └─ Health check                  │ │
│  │  └─ Error handler middleware         │ │
│  └──────────────┬────────────────────────┘ │
│                 │                           │
│  ┌──────────────▼────────────────────────┐ │
│  │  Middleware                           │ │
│  │  ├─ CORS (cross-origin requests)      │ │
│  │  ├─ JSON parser                       │ │
│  │  ├─ Validation (express-validator)    │ │
│  │  └─ Error handling                    │ │
│  └──────────────┬────────────────────────┘ │
│                 │                           │
│  ┌──────────────▼────────────────────────┐ │
│  │  Database Connection                  │ │
│  │  ├─ config/db.js                      │ │
│  │  │  └─ MongoDB Mongoose connection   │ │
│  │  └─ Environment variables (.env)      │ │
│  └──────────────┬────────────────────────┘ │
│                 │                           │
│  ┌──────────────▼────────────────────────┐ │
│  │  Data Models                          │ │
│  │  └─ models/Contact.js                │ │
│  │     ├─ name (String)                 │ │
│  │     ├─ email (String)                │ │
│  │     ├─ phone (String)                │ │
│  │     ├─ message (String)              │ │
│  │     └─ timestamps                    │ │
│  └──────────────────────────────────────┘ │
│                                            │
└────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### Form Submission Flow

```
User Input (Contact Page)
        │
        ▼
Frontend Validation
(HTML5 + React)
        │
        ▼
API Call: axios.post('/api/contact', data)
        │
        │──────────────────────────────┐
        │                              │
        │                    Backend (Express)
        │                              │
        ▼                              ▼
   Browser                    Request Handler
   (pending)            (routes/contacts.js)
                               │
                               ▼
                    Validation Middleware
                    (express-validator)
                               │
                      ┌────────┴────────┐
                      │                 │
                   Valid            Invalid
                      │                 │
                      ▼                 ▼
              Database Write     Error Response
              (MongoDB/         (400 + error)
               Mongoose)               │
                      │                 │
                      ▼                 │
              Success Response   ┌──────┘
              (201 + data)       │
                      │          │
                      └────┬─────┘
                           │
                           ▼
                    Frontend Response
                    ├─ Success: Show message
                    └─ Error: Show error
```

---

## Data Structure

### Contact Form Data
```
┌─ Contact Submission ─────────────────┐
│                                      │
│  name: String (required)             │
│  email: String (required, email)     │
│  phone: String (optional)            │
│  message: String (required)          │
│  status: String (new|read|replied)   │
│  createdAt: Timestamp                │
│  updatedAt: Timestamp                │
│                                      │
└──────────────────────────────────────┘
```

### Service Data
```
┌─ Service ────────────────────────────┐
│                                      │
│  id: Number                          │
│  name: String                        │
│  description: String                 │
│  icon: String (emoji)                │
│  tools: Array<String>                │
│  features: Array<String>             │
│  projects: Array<Project>            │
│                                      │
└──────────────────────────────────────┘
```

### Project Data
```
┌─ Project ───────────────────────────┐
│                                     │
│  id: Number                         │
│  name: String                       │
│  category: String                   │
│  description: String                │
│  image: URL String                  │
│  technologies: Array<String>        │
│  link: URL String                   │
│                                     │
└─────────────────────────────────────┘
```

### Testimonial Data
```
┌─ Testimonial ───────────────────────┐
│                                     │
│  id: Number                         │
│  name: String                       │
│  role: String                       │
│  company: String                    │
│  image: URL String                  │
│  rating: Number (1-5)               │
│  message: String                    │
│                                     │
└─────────────────────────────────────┘
```

---

## Technology Stack

```
┌─────────────────────────────────────────┐
│          TECH STACK VISUALIZATION        │
└─────────────────────────────────────────┘

FRONTEND                  BACKEND
────────────────────────────────────────

React 18              Node.js
  ├─ JSX             Express.js
  └─ Hooks             ├─ Routes
                       └─ Middleware

Vite                 MongoDB
  ├─ Fast builds      ├─ Collections
  └─ Hot reload       └─ Documents

React Router         Mongoose
  ├─ SPA routing      ├─ Schemas
  └─ Navigation       └─ Models

Tailwind CSS         Express Validator
  ├─ Utility classes  ├─ Form validation
  └─ Responsive      └─ Field validation

Framer Motion        CORS
  ├─ Animations      ├─ Cross-origin
  └─ Transitions     └─ Access control

Axios                Nodemon
  ├─ HTTP requests   ├─ Auto-restart
  └─ API calls       └─ Dev experience

PostCSS              Environment
  └─ CSS processing  └─ Config variables
```

---

## Component Hierarchy

```
App.jsx
│
├── Navbar
│   ├── Logo
│   ├── NavLinks
│   └── MobileMenu
│
├── Page Components
│   │
│   ├── Home.jsx
│   │   ├── Hero Section
│   │   ├── Services Preview (Card x3)
│   │   ├── Featured Projects (Card x3)
│   │   ├── Testimonials (Card x3)
│   │   └── CTA Section
│   │
│   ├── About.jsx
│   │   ├── Hero
│   │   ├── Mission/Vision
│   │   ├── Values (Card x4)
│   │   ├── Process Steps (Card x5)
│   │   └── Why Choose Us
│   │
│   ├── Services.jsx
│   │   ├── Hero
│   │   ├── Service Cards (Card x3)
│   │   │   └── Opens Modal on click
│   │   └── Modal
│   │       ├─ Service Details
│   │       ├─ Features List
│   │       ├─ Technologies (Badge)
│   │       └─ Sample Projects
│   │
│   ├── Projects.jsx
│   │   ├── Hero
│   │   ├── Filter Buttons
│   │   ├── Project Grid (Card x6)
│   │   └── CTA
│   │
│   ├── Testimonials.jsx
│   │   ├── Hero
│   │   ├── Featured Testimonial (Card)
│   │   │   └── Carousel Controls
│   │   ├── All Testimonials (Card x6)
│   │   ├── Statistics
│   │   └── CTA
│   │
│   └── Contact.jsx
│       ├── Hero
│       ├── Contact Info
│       ├── Contact Form
│       │   ├─ Input: Name
│       │   ├─ Input: Email
│       │   ├─ Input: Phone
│       │   ├─ Textarea: Message
│       │   └─ Submit Button
│       └── Map Placeholder
│
└── Footer
    ├── Brand Section
    ├── Quick Links
    ├── Services
    ├── Connect Section
    ├── Social Links
    └── Copyright
```

---

## Request/Response Flow

### Form Submission
```
Frontend                          Backend
   │                              │
   │─ User fills form            │
   │─ Validates (client-side)    │
   │─ POST /api/contact ─────────>│
   │                    (FormData)│
   │                              │ Receive
   │                              │ Validate (server)
   │                              │ Check DB rules
   │                              │ Save to MongoDB
   │                              │
   │<───────────────── 201 + Data │
   │                  Success msg │
   │─ Show message to user       │
   │
```

### Navigation
```
User Click                    Frontend
   │                              │
   │─ Click Link               │
   │─ React Router detects    │
   │─ URL changes             │
   │─ Component remounts       │
   │─ Animations trigger       │
   │─ Page displays            │
```

---

## Deployment Architecture (Future)

```
┌─────────────────────────────────────────────┐
│           DEPLOYMENT ARCHITECTURE            │
└─────────────────────────────────────────────┘

┌─ Frontend ──────────┐
│  Vercel/Netlify     │──┐
│  (static files)     │  │
└─────────────────────┘  │ Internet
                         │
                    ┌────▼───────────┐
                    │  User Browser   │
                    └────┬───────────┘
                         │
     ┌───────────────────┘
     │
┌────▼─────────────────┐
│  Backend (Heroku)    │
│  API Server          │
└────┬─────────────────┘
     │
┌────▼──────────────────────────┐
│  MongoDB Atlas (Cloud)         │
│  Database                      │
└───────────────────────────────┘
```

---

## Summary

This architecture provides:
- ✅ Separation of concerns (Frontend/Backend)
- ✅ Scalable component structure
- ✅ Clean API design
- ✅ Database persistence
- ✅ Error handling
- ✅ Production readiness

Perfect for a modern, professional digital agency website!

---

Last Updated: December 23, 2025
